# 深柴动力 1200kW 产品子站交接文档

更新日期：2026-07-24

## 项目定位

- 产品：1200kW 开架式柴油发电机组
- 技术：React + Three.js + Vite
- 默认语言：英文，可切换中文
- 源码入口：`src/App.jsx`、`src/App.css`
- 静态资源：`public/`
- 线上部署目录：`dist/`

## 当前页面能力

- 深柴动力统一蓝色品牌、字体与顶部导航。
- 3D 产品滚动展示与产品站切换入口。
- 全屏淡蓝色光晕随页面进度平滑变化，不显示生硬光束路径。
- 粒子、按钮、导航和强调色已移除原橙色体系。
- 最后一屏为询价页面，使用 `public/inquiry-factory.png`，带半透明遮罩和由下向上的进场动效。
- 页尾和获取报价入口已接入统一主站导航体系。

## 本次校正

- 品牌文案由“深柴电力”统一为“深柴动力”。
- 功率单位由错误的 `1200kV/1200kv` 校正为 `1200kW`。

## 2026-07-24 顶部导航统一

- 顶部导航已与主站统一，产品中心为当前栏目；中英文栏目名称、目标链接、Logo、语言、社交和报价按钮均采用全站标准。
- 桌面端导航高度 `70px`、Logo `36px`；移动端导航高度 `64px`、Logo `28px`。
- 报价按钮为固定 `6px` 圆角，导航位于 3D 预加载层之上，模型加载时不再出现导航变暗。
- 已重新构建 `dist/` 并通过桌面端、手机端自动化检查。

## 2026-07-24 跨站语言保持

- 新增 `src/languageRouting.js`，通过 `?lang=zh/en` 在独立 Cloudflare Pages 域名之间传递语言，并在当前域名保存本地语言记录。
- 顶部导航、产品切换器、询价入口及返回主站链接都会携带当前语言；直接使用带语言参数的网址进入时也会自动切换。
- 中英文切换按钮通过 `aria-pressed` 标记当前状态，支持键盘操作。
- 已重新构建 `dist/`，并通过主站 → 产品站 → 关于我们站的浏览器联动检查。

## 构建与部署

```powershell
pnpm install
pnpm run build
```

构建后直接部署 `dist/`。2026-07-22 已重新构建并验证。
